packages:
  - '.' # Or your existing workspace mapping array

minimumReleaseAge: 10080 # Delays installs of brand new versions by 7 days
trustPolicy:
  no-downgrade: true # Rejects dependency updates if trust signals weaken